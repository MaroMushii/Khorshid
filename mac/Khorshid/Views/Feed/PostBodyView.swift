import SwiftUI

// Stub — Agent 1 replaces this with SwiftSoup HTML rendering.
struct PostBodyView: View {

    let html: String

    var body: some View {
        Text(html)
            .lineLimit(3)
    }
}
